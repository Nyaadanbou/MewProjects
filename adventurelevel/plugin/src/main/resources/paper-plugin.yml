name: AdventureLevel
version: ${version}
description: ${description}
main: cc.mewcraft.adventurelevel.AdventureLevelPlugin
api-version: 1.19
authors: [ Nailm ]
dependencies:
  bootstrap: {}
  server:
    helper:
      load: BEFORE
      required: true
    helper-redis:
      load: BEFORE
      required: true
    helper-sql:
      load: BEFORE
      required: true
    MewCore:
      load: BEFORE
      required: true
    PlaceholderAPI:
      required: false
    MiniPlaceholders:
      required: false
    LuckPerms:
      required: false
