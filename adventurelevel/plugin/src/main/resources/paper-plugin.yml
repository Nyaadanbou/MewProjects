name: AdventureLevel
version: ${version}
description: ${description}
main: cc.mewcraft.adventurelevel.AdventureLevelPlugin
api-version: 1.19
authors: [ Nailm ]
dependencies:
  bootstrap: {}
  server:
    helper:
      load: BEFORE
      required: true
      join-classpath: true
    helper-redis:
      load: BEFORE
      required: true
      join-classpath: true
    helper-sql:
      load: BEFORE
      required: true
      join-classpath: true
    MewCore:
      load: BEFORE
      required: true
      join-classpath: true
    PlaceholderAPI:
      load: OMIT
      required: true
      join-classpath: true
    MiniPlaceholders:
      load: OMIT
      required: true
      join-classpath: true
    LuckPerms:
      load: OMIT
      required: true
      join-classpath: true
