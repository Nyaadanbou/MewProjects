name: TownyLink
version: ${version}
description: ${description}
main: cc.mewcraft.townylink.TownyLinkPlugin
api-version: 1.19
authors: [ Nailm ]
dependencies:
  bootstrap: { }
  server:
    helper:
      load: BEFORE
      required: true
      join-classpath: true
    helper-redis:
      load: BEFORE
      required: true
      join-classpath: true
    MewCore:
      load: BEFORE
      required: true
      join-classpath: true
    LuckPerms:
      load: OMIT
      required: true
      join-classpath: true
    Towny:
      load: OMIT
      required: false
      join-classpath: true
    HuskHomes:
      load: OMIT
      required: false
      join-classpath: true
